# Jules Sandbox – An Open‑Ended AI Playground

> **⚠️ Jules: this README must be updated at the end of every sprint.**
> It is the first thing humans see. Keep the Sprint Status and What's Been Built
> sections accurate and current. See the *Sprint‑End Update Protocol* section below.

Welcome to **Jules Sandbox**, a deliberately vague, self‑evolving codebase
intended for an autonomous AI (named *Jules*) to explore, build, and maintain
any kind of software it finds interesting.

## Current Sprint Status

| Field | Value |
|-------|-------|
| **Sprint** | — |
| **Status** | 🟡 In progress |
| **Last updated** | — |
| **Updated by** | Jules (autonomous) |

## What's Been Built

_Jules: keep this section current. One bullet per meaningful component._

- `examples/hello_world/` — minimal Python entry point
- `config/personas/` — 15 Sovereign Agentic OS personas + shared universal mandates
- `governance/templates/` — Eleven Hats PR review protocol
- `docs/` — GitHub Pages static preview site (`docs/index.md`)

## How It Works

- **Jules** runs on a schedule (or manually) and may add, modify, or delete
  files throughout the repository.
- The only hard constraints are the *Safety Rules* in `jules_policy.yaml`:
  no illegal, NSFW, or nefarious content, and no climate‑politics or story‑writing
  tools.
- Everything else is up to Jules' curiosity.  It could become a data‑science
  library, a tiny game, a collection of CLI utilities, or anything in‑between.

## Repository Layout

- `agents/` – reusable AI‑agent classes or notebooks.
- `tools/` – experimental utilities (scripts, binaries, notebooks, etc.).
- `examples/` – small demonstrators; the first one is a *Hello‑World* CLI app.
- `docs/` – **GitHub Pages static site** (project preview), plus roadmap, changelog, and design notes.
- `tests/` – unit‑test scaffolding; Jules will add real tests as it writes code.
- `logs/` – automatic records of policy checks and ethics decisions.
- `config/personas/` – embedded Sovereign Agentic OS personas plus the shared mandates that govern every persona prompt.
- `governance/templates/` – PR review protocols Jules must apply (Eleven Hats review).

## Getting Started (human side)

```bash
# Clone the repo
git clone <repo‑url>
cd jules-sandbox

# (Optional) Set up a virtual environment
python -m venv .venv
source .venv/bin/activate

# Install the first example's dependencies
pip install -r examples/hello_world/requirements.txt
```

Run the example:

```bash
python examples/hello_world/main.py
```

You should see:

```
Hello, world! (generated by Jules)
```

## Personas & Mandates

The `config/personas/` directory contains the full Sovereign Agentic OS persona pack (Arbiter, Sentinel, Palette, Consolidator, etc.) plus the shared universal mandates. Jules can load these verbatim to bootstrap multi‑persona workflows without reaching outside the repo.

The `governance/templates/eleven_hats_review.md` file is the **mandatory PR review template** — Jules must apply the Eleven Hats evaluation to every PR it submits.

## GitHub Pages — Live Project Preview

This repository's `/docs` folder is published as a **static GitHub Pages site**.  
The live preview is always available at the GitHub Pages URL for this repo.

## Sprint‑End Update Protocol

At the end of every sprint Jules **must** update **both** of these files:

| File | Why |
|------|-----|
| `README.md` | First thing humans see on GitHub — must reflect current reality |
| `docs/index.md` | GitHub Pages static site — public sprint-review display |

For each file, update:
- **Current Sprint Status** table (sprint number, status, date)
- **What's Been Built / What Jules Has Built So Far** (all new components)
- **Active Roadmap Items** checklist (keep in sync with `docs/roadmap.md`)
- **Sprint History** table (append a new row)

Commit both files together with the message: `docs: sprint <N> preview update`

See `jules_config.yaml` → `github_pages` for the machine-readable version of this obligation.

---

*Feel free to watch the repo evolve!  Jules will commit new ideas,
experiments, and refinements over time.*
